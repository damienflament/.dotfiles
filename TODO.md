# Things To Do

## Terminal
- Gnome Terminal Colors

## Dotfiles
- Command to update *.gitignore* file from a more user friendly configuration file

## Zsh
- Automatically compile local functions (using `zcompile`)
- Trash mode for `rm` command (or `trash` command)
- Use zsh/system syserror
- Add context option (-c) to zerror to show custom context instead of caller

## Xonsh
- Source  `/etc/profile` from `rc.xsh`
- ~~Prevent duplicates in $PATH (keep the first found value)~~
- Use `scoping` package in RC file instead of `del`eting local variables
- Find a good pager with syntax highlighting (like `vimpager` and `vimcat`)
- Use a `command_available` function like in Zsh configuration
- Setup prompt colors depending on the UI style (light or dark)

## System
- Media buttons management via daemon **[IMPROVE]**
